version: '3.7'
services:
  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: venly
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: sooper-dooper-secure